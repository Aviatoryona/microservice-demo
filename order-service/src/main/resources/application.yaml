server:
  port: 8083

spring:
  application:
    name: ORDER-SERVICE
  datasource:
    url: jdbc:postgresql://172.17.0.1:5432/mscdemo?currentSchema=orderservice
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

product:
  service:
    url: http://PRODUCT-SERVICE

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka